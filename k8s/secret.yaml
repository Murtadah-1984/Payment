apiVersion: v1
kind: Secret
metadata:
  name: payment-secrets
  namespace: payment
type: Opaque
stringData:
  ConnectionStrings__DefaultConnection: "Host=postgres-service;Port=5432;Database=PaymentDb;Username=postgres;Password=CHANGE_ME"
  # JWT authentication is now handled by external Identity Microservice
  # No local secret key is required - tokens are validated against the Identity Microservice's JWKS endpoint

